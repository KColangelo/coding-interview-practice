[project]
name = "ds-interview-prep"
version = "0.1.0"
description = "Canonical walkthroughs for data scientist coding interviews"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "scikit-learn",
    "statsmodels",
    "econml",
    "matplotlib",
]
